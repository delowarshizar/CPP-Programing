#include<bits/stdc++.h>
using namespace std;
int main()
{
    double height,weight,area;
    cout<<"Enter height: ";
    cin>>height;
    cout<<"Enter weight: ";
    cin>>weight;
    area = height * weight;
    cout<<showpoint;
    cout<<fixed;
    cout<<setprecision(3);
    cout<<"Area of the rectangle is : "<<area<<" cm";
    return 0;
}