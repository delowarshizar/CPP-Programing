#include<bits/stdc++.h>
using namespace std;
int main()
{
    int base,height;
    cout<<"Enter Base : ";
    cin>>base;
    cout<<"Enter Height: ";
    cin>>height;
    float area = 0.5 * base * height ;
    cout<<showpoint;
    cout<<fixed;
    cout<<setprecision(2);
    cout<<"The area of this triangle is : "<<area<<"cm"<<endl;
}